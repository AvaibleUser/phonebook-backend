{
  "name": "phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../phonebook-frontend && npm run build && mv build ../phonebook-backend/static",
    "deploy": "...",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "..."
  },
  "dependencies": {
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
