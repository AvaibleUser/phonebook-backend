{
  "name": "phonebook",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf static && cd ../phonebook-frontend && npm run build && mv build ../phonebook-backend/static",
    "deploy": "...",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "..."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.8.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
